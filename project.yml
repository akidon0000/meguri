name: Meguri
options:
  bundleIdPrefix: com.akidon0000
  deploymentTarget:
    iOS: "26.0"
  developmentLanguage: en
  xcodeVersion: "27.0"
  createIntermediateGroups: true
  generateEmptyDirectories: true

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete
    DEVELOPMENT_TEAM: XSC9AJPSP3
    CODE_SIGN_STYLE: Automatic
    ENABLE_USER_SCRIPT_SANDBOXING: YES
    SWIFT_UPCOMING_FEATURE_EXISTENTIAL_ANY: YES

targets:
  Meguri:
    type: application
    platform: iOS
    sources:
      - path: Meguri
    info:
      path: Meguri/Info.plist
      properties:
        CFBundleDisplayName: Meguri
        CFBundleShortVersionString: "1.0"
        CFBundleVersion: "1"
        UILaunchScreen: {}
        UISupportedInterfaceOrientations: [UIInterfaceOrientationPortrait]
        ITSAppUsesNonExemptEncryption: false
        NSCameraUsageDescription: "作品や風景を撮影して、その場で説明を生成するために使います。"
        NSLocationWhenInUseUsageDescription: "撮影した場所の名前を記録し、説明の手がかりにするために使います。"
        NSPhotoLibraryAddUsageDescription: "撮影した写真を保存するために使います。"
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.akidon0000.meguri
        TARGETED_DEVICE_FAMILY: "1"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        SWIFT_EMIT_LOC_STRINGS: YES
    scheme:
      testTargets:
        - MeguriTests
      gatherCoverageData: true

  MeguriTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: MeguriTests
    dependencies:
      - target: Meguri
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.akidon0000.meguri.tests
        GENERATE_INFOPLIST_FILE: YES
